with open('packets.txt', 'r') as f:
    // your code

packet_list = []
packet_start = 0

while packet_start < len(packet_stream):
    // your code

# print the tuples
for packet in packet_list:
    print(packet)
